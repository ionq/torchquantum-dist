[project]
name = "torchquantum-dist"
requires-python = ">= 3.9"
dynamic = ["version"]
dependencies = [
    "torch>=2.7",
]

[tool.setuptools]
packages = ["tqd"]

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"